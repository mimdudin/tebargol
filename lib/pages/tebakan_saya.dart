import 'package:flutter/material.dart';

class TebakanSaya extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:  AppBar(
        title: Text('Tebakan Saya')
      ),
      body: Center(
         child: Text('Tebakan Saya page'),
      ),
    );
  }
}