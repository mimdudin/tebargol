import 'package:flutter/material.dart';

class LineupMatchTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
         child: Text('Line up match tab'),
      );
  }
}