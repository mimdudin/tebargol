import 'package:flutter/material.dart';

class Peraturan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:  AppBar(
        title: Text('Peraturan')
      ),
      body: Center(
         child: Text('Peraturan page'),
      ),
    );
  }
}