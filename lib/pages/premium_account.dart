import 'package:flutter/material.dart';

class PremiumAccount extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:  AppBar(
        title: Text('Premium Account')
      ),
      body: Center(
         child: Text('Premium Account page'),
      ),
    );
  }
}