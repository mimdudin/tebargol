import 'package:flutter/material.dart';

class Hubungi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:  AppBar(
        title: Text('Hubungi')
      ),
      body: Center(
         child: Text('Hubungi page'),
      ),
    );
  }
}