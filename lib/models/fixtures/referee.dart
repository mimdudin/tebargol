class Referee {
  final int id;
  final String fullname;

  Referee({this.id, this.fullname});

    // factory Referee.fromJson(Map<String, dynamic> json){
    //   return Referee(id: json['id'], fullname: json['fullname']);
    // }
}